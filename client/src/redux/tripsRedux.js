
const initialState = {
    tripsData: [],
    loading: true,
    errMsg: ""
}

this.state = this.initialState;

const tripsReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}
export default tripsReducer