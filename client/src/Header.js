import React from 'react'

function Header(props) {
    return (
        <div>
            This is where we will call on the data from the user to dynamically render a photo of the destination the user entered as the header image.
        </div>
    )
}

export default Header
