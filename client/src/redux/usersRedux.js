
const initialState = {
    usersData: [],
    loading: true,
    errMsg: ""
}

this.state = this.initialState;

const usersReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}
export default usersReducer